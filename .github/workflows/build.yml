name: GarnetKernel Build

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - 'LICENSE'
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      ENABLE_LTO:
        description: 'Enable Thin LTO (true/false)'
        required: false
        default: 'true'
      ENABLE_SUSFS:
        description: 'Enable SUSFS patch (true/false)'
        required: false
        default: 'true'

# ─────────────────────────────────────────────────────────────────────────────
# VARIABLES
# ─────────────────────────────────────────────────────────────────────────────
env:
  # ── Kernel source ─────────────────────────────────────────
  # GKI android12-5.10 — misma base que Wild Kernel 5.10.236
  # Wild Kernel confirma que esta base arranca en garnet + HyperOS 3
  # La diferencia: Wild usa Clang 12 stock, nosotros usamos Neutron Clang 18
  KERNEL_REPO:   https://android.googlesource.com/kernel/common
  KERNEL_BRANCH: android12-5.10

  # ── KernelSU Next ─────────────────────────────────────────
  KSU_TAG: v3.2.0

  # ── SUSFS ─────────────────────────────────────────────────
  SUSFS_REPO:   https://gitlab.com/simonpunk/susfs4ksu.git
  SUSFS_BRANCH: gki-android12-5.10

  # ── Toolchain ─────────────────────────────────────────────
  CLANG_DIR: /home/runner/neutron-clang

  # ── Build identity ────────────────────────────────────────
  KERNEL_NAME:   GarnetKernel
  LOCALVERSION:  -GarnetKernel

jobs:
  build:
    name: Build GarnetKernel
    runs-on: ubuntu-24.04
    timeout-minutes: 180

    steps:

# ═════════════════════════════════════════════════════════════════════════════
# 1. CHECKOUT
# ═════════════════════════════════════════════════════════════════════════════
      - name: Checkout GarnetKernel repo
        uses: actions/checkout@v4
        with:
          path: garnet-scripts

# ═════════════════════════════════════════════════════════════════════════════
# 2. DEPENDENCIAS
# ═════════════════════════════════════════════════════════════════════════════
      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            bc bison build-essential ca-certificates cpio flex \
            git kmod libelf-dev libssl-dev lld llvm python3 \
            zip curl wget ccache pahole zstd \
            gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi

# ═════════════════════════════════════════════════════════════════════════════
# 3. CCACHE
# ═════════════════════════════════════════════════════════════════════════════
      - name: Setup ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          max-size: 8G
          key: garnet-gki-clang18-${{ github.ref_name }}
          restore-keys: |
            garnet-gki-clang18-
            garnet-gki-

# ═════════════════════════════════════════════════════════════════════════════
# 4. NEUTRON CLANG 18
# ═════════════════════════════════════════════════════════════════════════════
      - name: Cache Neutron Clang
        id: cache-clang
        uses: actions/cache@v4
        with:
          path: ${{ env.CLANG_DIR }}
          key: neutron-clang-antman-latest-v3

      - name: Download Neutron Clang via antman
        if: steps.cache-clang.outputs.cache-hit != 'true'
        run: |
          mkdir -p ${{ env.CLANG_DIR }}
          cd ${{ env.CLANG_DIR }}
          curl -LO "https://raw.githubusercontent.com/Neutron-Toolchains/antman/main/antman"
          chmod +x antman
          ./antman -S=latest
          echo "✅ Clang ready:"
          bin/clang --version

      - name: Export toolchain to PATH
        run: |
          echo "${{ env.CLANG_DIR }}/bin" >> $GITHUB_PATH
          echo "CLANG_PATH=${{ env.CLANG_DIR }}" >> $GITHUB_ENV
          clang --version

# ═════════════════════════════════════════════════════════════════════════════
# 5. KERNEL SOURCE — GKI android12-5.10
# ═════════════════════════════════════════════════════════════════════════════
      - name: Clone GKI kernel source
        run: |
          git clone --depth=1 \
            -b ${{ env.KERNEL_BRANCH }} \
            ${{ env.KERNEL_REPO }} kernel

          cd kernel
          KHEAD=$(git rev-parse --short HEAD)
          echo "KERNEL_HEAD=$KHEAD" >> $GITHUB_ENV
          echo "✅ Source: $KHEAD — $(git log --oneline -1)"

# ═════════════════════════════════════════════════════════════════════════════
# 6. KERNELSU NEXT v3.2.0
# GKI puro — kprobes disponibles, no hace falta parchear
# ═════════════════════════════════════════════════════════════════════════════
      - name: Integrate KernelSU Next v3.2.0
        run: |
          cd kernel
          curl -LSs \
            "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh" \
            | bash -s ${{ env.KSU_TAG }}

          KSU_DIR=""
          [ -d "KernelSU-Next" ] && KSU_DIR="KernelSU-Next"
          [ -d "KernelSU" ]      && KSU_DIR="KernelSU"

          if [ -z "$KSU_DIR" ]; then
            echo "❌ KernelSU integration failed"
            exit 1
          fi

          KSU_VER=$(cat "$KSU_DIR/VERSION" 2>/dev/null || echo "${{ env.KSU_TAG }}")
          echo "KSU_VER=$KSU_VER" >> $GITHUB_ENV
          echo "KSU_DIR=$KSU_DIR" >> $GITHUB_ENV
          echo "✅ KernelSU Next ($KSU_DIR) — $KSU_VER"

# ═════════════════════════════════════════════════════════════════════════════
# 7. SUSFS v2.0.0
# ═════════════════════════════════════════════════════════════════════════════
      - name: Clone SUSFS
        if: ${{ github.event.inputs.ENABLE_SUSFS != 'false' }}
        run: |
          git clone --depth=1 \
            -b ${{ env.SUSFS_BRANCH }} \
            ${{ env.SUSFS_REPO }} susfs

          SUSFS_VER=$(cat susfs/VERSION 2>/dev/null || echo "v2.0.0")
          echo "SUSFS_VER=$SUSFS_VER" >> $GITHUB_ENV
          echo "✅ SUSFS cloned — $SUSFS_VER"

      - name: Apply SUSFS patch
        if: ${{ github.event.inputs.ENABLE_SUSFS != 'false' }}
        run: |
          cd kernel

          # Copiar archivos fuente
          cp ../susfs/kernel_patches/fs/susfs.c         fs/            2>/dev/null && echo "✓ fs/susfs.c"
          cp ../susfs/kernel_patches/fs/sus_su.c        fs/            2>/dev/null && echo "✓ fs/sus_su.c"
          cp ../susfs/kernel_patches/include/linux/susfs.h  include/linux/ 2>/dev/null && echo "✓ susfs.h"
          cp ../susfs/kernel_patches/include/linux/sus_su.h include/linux/ 2>/dev/null && echo "✓ sus_su.h"

          # Buscar patch principal
          PATCH=""
          for candidate in \
            "$GITHUB_WORKSPACE/susfs/kernel_patches/50_add_susfs_in_gki-android12-5.10.patch" \
            "$GITHUB_WORKSPACE/susfs/kernel_patches/50_add_susfs_in_kernel-5.10.patch"; do
            [ -f "$candidate" ] && PATCH="$candidate" && break
          done
          [ -z "$PATCH" ] && PATCH=$(find "$GITHUB_WORKSPACE/susfs/kernel_patches/" \
            -name "*5.10*.patch" 2>/dev/null | head -1)

          if [ -z "$PATCH" ]; then
            echo "❌ SUSFS patch not found"
            ls "$GITHUB_WORKSPACE/susfs/kernel_patches/"
            exit 1
          fi

          echo "Applying: $(basename $PATCH)"
          patch -p1 --forward < "$PATCH" && \
            echo "✅ SUSFS patch applied" || {
            patch -p1 --forward --reject-file=/tmp/susfs.rej < "$PATCH" || true
            if find . -name "*.rej" 2>/dev/null | grep -q .; then
              echo "❌ SUSFS patch failed — .rej files:"
              cat /tmp/susfs.rej 2>/dev/null | head -60
              exit 1
            fi
            echo "⚠ SUSFS applied with minor conflicts"
          }

          # KSU bridge patch
          KSU_PATCH=$(find "$GITHUB_WORKSPACE/susfs/kernel_patches/" \
            \( -name "*ksu*" -o -name "*kernelsu*" \) 2>/dev/null | head -1)
          if [ -f "$KSU_PATCH" ] && [ -n "$KSU_DIR" ]; then
            echo "Applying KSU bridge: $(basename $KSU_PATCH)"
            ( cd "$KSU_DIR" && patch -p1 --forward < "$KSU_PATCH" ) && \
              echo "✅ KSU bridge applied" || \
              echo "⚠ KSU bridge skipped (already applied)"
          fi

# ═════════════════════════════════════════════════════════════════════════════
# 8. CUSTOM PATCHES
# ═════════════════════════════════════════════════════════════════════════════
      - name: Apply custom patches
        run: |
          cd kernel
          PDIR="${{ github.workspace }}/garnet-scripts/patches"
          if [ -d "$PDIR" ] && ls "$PDIR"/*.patch 2>/dev/null; then
            for p in "$PDIR"/*.patch; do
              echo "Applying: $(basename $p)"
              patch -p1 --forward < "$p" && echo "  ✅ OK" || \
                echo "  ⚠ skipped (already applied)"
            done
          else
            echo "No custom patches — skipping"
          fi

# ═════════════════════════════════════════════════════════════════════════════
# 9. DEFCONFIG — gki_defconfig + overlay de optimizaciones
# ═════════════════════════════════════════════════════════════════════════════
      - name: Generate optimized defconfig
        run: |
          cd kernel
          mkdir -p out

          echo "=== Base: gki_defconfig ==="
          make O=out ARCH=arm64 gki_defconfig

          echo "=== GarnetKernel overlay ==="

          # ── KernelSU Next ──────────────────────────────────────────
          scripts/config --file out/.config \
            -e KSU \
            -e KSU_DEBUG

          # ── SUSFS v2.0.0 ───────────────────────────────────────────
          scripts/config --file out/.config \
            -e KSU_SUSFS \
            -e KSU_SUSFS_SUS_PATH \
            -e KSU_SUSFS_SUS_MOUNT \
            -e KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT \
            -e KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT \
            -e KSU_SUSFS_SUS_KSTAT \
            -e KSU_SUSFS_SUS_OVERLAYFS \
            -e KSU_SUSFS_TRY_UMOUNT \
            -e KSU_SUSFS_SPOOF_UNAME \
            -e KSU_SUSFS_ENABLE_LOG \
            -e KSU_SUSFS_OPEN_REDIRECT \
            -e KSU_SUSFS_SUS_SU

          # ── CPU Scheduler: WALT + EAS + uclamp ────────────────────
          scripts/config --file out/.config \
            -e SCHED_WALT \
            -e SCHED_CORE_CTL \
            -e CPU_BOOST \
            -e UCLAMP_TASK \
            -e UCLAMP_TASK_GROUP \
            --set-val UCLAMP_BUCKETS_COUNT 5 \
            -e ENERGY_MODEL \
            -e CPU_FREQ_GOV_SCHEDUTIL \
            -e CPU_FREQ_GOV_PERFORMANCE \
            -e CPU_FREQ_GOV_POWERSAVE \
            -d CPU_FREQ_GOV_ONDEMAND \
            -d CPU_FREQ_GOV_CONSERVATIVE \
            -e CPU_IDLE \
            -e CPU_IDLE_GOV_MENU \
            -e ARM_PSCI_CPUIDLE

          # ── GPU ────────────────────────────────────────────────────
          scripts/config --file out/.config \
            -e DEVFREQ_GOV_SIMPLE_ONDEMAND \
            -e DEVFREQ_GOV_PERFORMANCE

          # ── I/O: Kyber para UFS 3.1 ───────────────────────────────
          scripts/config --file out/.config \
            -e MQ_IOSCHED_KYBER \
            -e MQ_IOSCHED_DEADLINE \
            -d IOSCHED_CFQ

          # ── Memory: zRAM zstd ──────────────────────────────────────
          scripts/config --file out/.config \
            -e ZRAM \
            -e ZSMALLOC \
            -e CRYPTO_LZ4 \
            -e CRYPTO_ZSTD \
            -e ZRAM_DEF_COMP_ZSTD \
            -e TRANSPARENT_HUGEPAGE \
            -e TRANSPARENT_HUGEPAGE_MADVISE

          # ── TCP: BBR + FQ ──────────────────────────────────────────
          scripts/config --file out/.config \
            -e TCP_CONG_BBR \
            -e NET_SCH_FQ \
            -e NET_SCH_FQ_CODEL \
            --set-str DEFAULT_TCP_CONG bbr

          # ── Thin LTO ──────────────────────────────────────────────
          if [ "${{ github.event.inputs.ENABLE_LTO }}" != "false" ]; then
            scripts/config --file out/.config \
              -d LTO_NONE \
              -e LTO_CLANG \
              -e THINLTO
            echo "Thin LTO: ENABLED"
          fi

          # ── Security ──────────────────────────────────────────────
          scripts/config --file out/.config \
            -e SECURITY_SELINUX \
            -e SECURITY_SELINUX_BOOTPARAM \
            -d SECURITY_SELINUX_DEVELOP \
            -e RANDOMIZE_BASE \
            -e RANDOMIZE_MODULE_REGION_FULL

          # ── Localversion ──────────────────────────────────────────
          scripts/config --file out/.config \
            --set-str LOCALVERSION "${{ env.LOCALVERSION }}"

          # Resolver dependencias
          make O=out ARCH=arm64 olddefconfig

          echo "=== Key configs ==="
          grep -E "^CONFIG_(KSU|SUSFS|THINLTO|LTO|SCHED_WALT|UCLAMP|ZRAM|KYBER|BBR)" \
            out/.config || true

# ═════════════════════════════════════════════════════════════════════════════
# 10. BUILD
# KCFLAGS optimizados para SM7435 (4x Cortex-A78 @ 2.4GHz + 4x Cortex-A55)
# Wild Kernel usaba -march=armv8.2-a con Clang 12 stock
# Nosotros añadimos -mcpu=cortex-a78 (Clang 18 lo soporta) para el pipeline A78
# ═════════════════════════════════════════════════════════════════════════════
      - name: Build GarnetKernel
        run: |
          cd kernel
          BUILD_START=$(date +%s)
          echo "Build started: $(date)"

          make -j$(nproc --all) \
            O=out \
            ARCH=arm64 \
            CC="ccache clang" \
            LD=ld.lld \
            AR=llvm-ar \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            STRIP=llvm-strip \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_COMPAT=arm-linux-gnueabi- \
            LLVM=1 \
            LLVM_IAS=1 \
            KCFLAGS="-O3 -mcpu=cortex-a78 -mtune=cortex-a78 -march=armv8.2-a \
                     -Wno-error=frame-larger-than \
                     -Wno-error=unused-function" \
            KBUILD_BUILD_USER=Shammuray10 \
            KBUILD_BUILD_HOST=GarnetKernel \
            2>&1 | tee out/build.log

          DIFF=$(( $(date +%s) - BUILD_START ))
          echo "BUILD_TIME=$((DIFF/60))m $((DIFF%60))s" >> $GITHUB_ENV

          if [ -f out/arch/arm64/boot/Image ]; then
            echo "✅ Build OK — $((DIFF/60))m $((DIFF%60))s"
            ls -lh out/arch/arm64/boot/Image
          else
            echo "❌ BUILD FAILED"
            tail -100 out/build.log
            exit 1
          fi

# ═════════════════════════════════════════════════════════════════════════════
# 11. PACKAGE — AnyKernel3 con anykernel.sh de Wild Kernel
# El anykernel.sh de Wild está probado en garnet + HyperOS 3
# Solo cambiamos el branding y la Image
# ═════════════════════════════════════════════════════════════════════════════
      - name: Clone AnyKernel3
        run: |
          git clone --depth=1 \
            https://github.com/WildKernels/AnyKernel3.git anykernel
          rm -rf anykernel/.git

      - name: Package flashable ZIP
        run: |
          # Kernel image
          cp kernel/out/arch/arm64/boot/Image anykernel/

          # DTBs si existen
          [ -f kernel/out/arch/arm64/boot/dtb.img  ] && \
            cp kernel/out/arch/arm64/boot/dtb.img  anykernel/ || true
          [ -f kernel/out/arch/arm64/boot/dtbo.img ] && \
            cp kernel/out/arch/arm64/boot/dtbo.img anykernel/ || true

          # anykernel.sh desde el repo (basado en Wild Kernel, funciona en garnet)
          cp "${{ github.workspace }}/garnet-scripts/anykernel/anykernel.sh" anykernel/

          TIMESTAMP=$(date +%Y%m%d-%H%M)
          ZIPNAME="${{ env.KERNEL_NAME }}-garnet-${TIMESTAMP}-KSUNext-${{ env.KSU_TAG }}-SUSFS-${{ env.SUSFS_VER }}.zip"
          echo "ZIPNAME=$ZIPNAME" >> $GITHUB_ENV

          cd anykernel
          zip -r9 "../$ZIPNAME" . -x "*.git*" -x "*placeholder"
          cd ..

          echo "✅ ZIP: $ZIPNAME ($(du -h $ZIPNAME | cut -f1))"

# ═════════════════════════════════════════════════════════════════════════════
# 12. UPLOAD
# ═════════════════════════════════════════════════════════════════════════════
      - name: Upload kernel ZIP
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ZIPNAME }}
          path: ${{ env.ZIPNAME }}
          retention-days: 30

      - name: Upload build log (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ github.run_number }}
          path: kernel/out/build.log
          retention-days: 7

# ═════════════════════════════════════════════════════════════════════════════
# 13. SUMMARY
# ═════════════════════════════════════════════════════════════════════════════
      - name: Build Summary
        if: success()
        run: |
          echo "## ✅ GarnetKernel — Build OK" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Campo | Valor |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Device | Redmi Note 13 Pro 5G (2312DRA50G) |" >> $GITHUB_STEP_SUMMARY
          echo "| SoC | Snapdragon 7s Gen 2 (SM7435) |" >> $GITHUB_STEP_SUMMARY
          echo "| CPU | 4x Cortex-A78 @ 2.4GHz + 4x Cortex-A55 @ 1.95GHz |" >> $GITHUB_STEP_SUMMARY
          echo "| ROM target | HyperOS 3 stock |" >> $GITHUB_STEP_SUMMARY
          echo "| Kernel base | GKI ${{ env.KERNEL_BRANCH }} @ ${{ env.KERNEL_HEAD }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Base probada | Wild Kernel 5.10.236 (boot confirmado en garnet) |" >> $GITHUB_STEP_SUMMARY
          echo "| KernelSU Next | ${{ env.KSU_VER }} |" >> $GITHUB_STEP_SUMMARY
          echo "| SUSFS | ${{ env.SUSFS_VER }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Compiler | Neutron Clang 18 (vs Clang 12 de Wild) |" >> $GITHUB_STEP_SUMMARY
          echo "| KCFLAGS | -O3 -mcpu=cortex-a78 -mtune=cortex-a78 |" >> $GITHUB_STEP_SUMMARY
          echo "| LTO | Thin LTO |" >> $GITHUB_STEP_SUMMARY
          echo "| Scheduler | WALT + EAS + uclamp |" >> $GITHUB_STEP_SUMMARY
          echo "| I/O | Kyber (UFS 3.1) |" >> $GITHUB_STEP_SUMMARY
          echo "| TCP | BBR |" >> $GITHUB_STEP_SUMMARY
          echo "| zRAM | zstd |" >> $GITHUB_STEP_SUMMARY
          echo "| Build time | ${{ env.BUILD_TIME }} |" >> $GITHUB_STEP_SUMMARY
          echo "| ZIP | ${{ env.ZIPNAME }} |" >> $GITHUB_STEP_SUMMARY
          
